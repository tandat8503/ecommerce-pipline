data:
  raw_dir: data/raw
  staging_dir: data/staging
  warehouse_dir: data/warehouse

pipeline:
  min_price: 0.0
  valid_order_statuses:
    - delivered
    - shipped
    - canceled
    - processing
    - invoiced
    - unavailable
    - approved
    - created
  # Status dùng để tính revenue trong fact_orders
  revenue_status: delivered

bigquery:
  project_id: "your-gcp-project-id"
  dataset: "ecommerce_dwh"
  load_target: "local" # local | bigquery

logging:
  level: INFO
  log_dir: logs
